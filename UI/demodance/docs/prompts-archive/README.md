# Prompt Archive

These markdown files are historical prompt design references only.

Runtime prompts are defined in:
- `lib/server/story-prompts.ts`
- `lib/server/scene-prompts.ts`
- `lib/server/voice-prompts.ts`

Do not load these markdown files with `fs` at runtime on Vercel.
